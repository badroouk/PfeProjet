import 'package:flutter/material.dart';


class polPage extends StatelessWidget {
  const polPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xFF006E7F),
        title: Text('Polution data'),
      ),
      body:
      Column(

      ),
    );
  }
}
